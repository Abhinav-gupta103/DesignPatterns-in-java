package Decorator_Pattern;

// Step 1:- in this pattern the base component interface is implemented by both concrete and decorator classes
public interface Coffee {
    String getDescription();

    double getCost();
}
